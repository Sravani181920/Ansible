- name: install and run nginx
  hosts: Web
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start the niginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes