- name: variables fetching from file
  hosts: local
  connection: local
  vars_files: vars.yaml #variables are in vars.yaml file
  tasks:
  - name: print the variables
    ansible.builtin.debug:
      msg: "course is {{ COURSE }} and the duration is {{ DURATION }} trained by {{ TRAINER }}"